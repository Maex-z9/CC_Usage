---
phase: 02-system-tray-display
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "System tray icon is visible in GNOME panel"
    - "Icon shows circular gauge with correct color for usage level"
    - "Clicking icon shows menu with usage percentages and reset time"
    - "Tooltip shows usage summary on hover"
    - "Refresh menu item triggers data update"
    - "Quit menu item closes application"
  artifacts: []
  key_links: []
---

<objective>
Verify the system tray application works correctly through visual inspection and interaction testing.

Purpose: Confirm all Phase 2 requirements are met before marking the phase complete. This is a human verification checkpoint.
Output: Confirmation that tray icon, menu, tooltip, and colors all work as specified.
</objective>

<execution_context>
@/home/max/.claude/get-shit-done/workflows/execute-plan.md
@/home/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-system-tray-display/02-CONTEXT.md
@.planning/phases/02-system-tray-display/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
System tray application with:
- Circular gauge icon showing session usage percentage
- Color-coded icon (green < 50%, yellow 50-75%, red >= 75%)
- Click menu with "Session: X% | Weekly: Y%" and "Resets in {time}"
- Hover tooltip with brief summary
- Refresh, Settings (disabled), and Quit menu items
  </what-built>
  <how-to-verify>
1. **Start the application:**
   ```bash
   cd /home/max/Documents/GitHub/overlay-CC-usage
   python3 -m src.main
   ```

2. **Verify tray icon appears:**
   - Look for circular gauge icon in GNOME top bar (system tray area, usually right side)
   - If not visible, ensure "AppIndicator and KStatusNotifierItem Support" GNOME extension is installed and enabled

3. **Verify icon color:**
   - Icon should be green if your usage is < 50%
   - Icon should be yellow if usage is 50-75%
   - Icon should be red if usage is >= 75%
   - Color is based on the HIGHER of session/weekly (worst-case)

4. **Verify tooltip:**
   - Hover over the tray icon
   - Should show: "Session: X% | Weekly: Y%" (no app name, just data)

5. **Verify menu (click the icon):**
   - First line: "Session: X%  |  Weekly: Y%" (non-clickable, grayed)
   - Second line: "Resets in {time}" like "Resets in 2 hours" (non-clickable)
   - Separator line
   - "Refresh" (clickable - click it and watch for icon update)
   - "Settings" (grayed out - placeholder for Phase 4)
   - Separator line
   - "Quit" (clickable)

6. **Test Refresh:**
   - Click "Refresh" in menu
   - Icon should briefly update (may not change color if usage hasn't changed)

7. **Test Quit:**
   - Click "Quit" in menu
   - Application should close (icon disappears from tray)

**Expected result:** All 7 checks pass. Report any issues.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all Phase 2 requirements are working:
- TRAY-01: System tray icon visible in GNOME panel
- TRAY-02: Click icon to see menu with current usage details
- TRAY-03: Hover tooltip shows quick usage summary
- TRAY-04: Color-coded icon (green/yellow/red based on highest usage level)
- DISP-01: Show session % (five_hour utilization)
- DISP-02: Show weekly % (seven_day utilization)
- DISP-03: Show time remaining until reset
</verification>

<success_criteria>
- User confirms tray icon is visible
- User confirms menu shows correct data
- User confirms tooltip works
- User confirms color coding works
- User confirms Refresh and Quit work
</success_criteria>

<output>
After completion, create `.planning/phases/02-system-tray-display/02-03-SUMMARY.md`
</output>

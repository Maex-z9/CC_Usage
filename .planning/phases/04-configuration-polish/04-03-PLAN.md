---
phase: 04-configuration-polish
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can configure thresholds per metric"
    - "User can configure polling interval"
    - "User can trigger force refresh via menu"
    - "User can enable pause notifications mode"
    - "Application auto-starts on login via .desktop file"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint: Confirm all Phase 4 configuration features work correctly through hands-on testing.

Purpose: Ensure the configuration system is user-ready before marking phase complete.
Output: User-verified, working configuration and autostart functionality.
</objective>

<execution_context>
@/home/max/.claude/get-shit-done/workflows/execute-plan.md
@/home/max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-configuration-polish/04-01-SUMMARY.md
@.planning/phases/04-configuration-polish/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete configuration system with Settings menu, autostart support, configurable thresholds and polling interval</what-built>
  <how-to-verify>
**Test 1: Settings Menu Structure**
1. Run `python3 /home/max/Documents/GitHub/overlay-CC-usage/src/main.py`
2. Click tray icon to open menu
3. Verify "Settings" submenu contains:
   - [ ] "Pause Notifications" checkbox
   - [ ] "Autostart on Login" checkbox
   - [ ] "Edit Config File..." item
4. Verify "Refresh" menu item shows Ctrl+R shortcut indicator

**Test 2: Pause Notifications Toggle**
1. In Settings submenu, click "Pause Notifications" to enable
2. Verify `~/.config/claude-usage-overlay/config.json` shows `"pause_notifications": true`
3. Toggle off, verify config updates to `false`

**Test 3: Autostart Toggle**
1. In Settings submenu, click "Autostart on Login" to enable
2. Verify file exists: `ls ~/.config/autostart/claude-usage-overlay.desktop`
3. Verify file content shows `Hidden=false` or no Hidden line
4. Toggle off, verify file is removed or Hidden=true

**Test 4: Edit Config File**
1. Click "Edit Config File..." in Settings submenu
2. Verify text editor opens with config.json
3. Verify file shows readable JSON with all settings

**Test 5: Custom Polling Interval (requires restart)**
1. Edit `~/.config/claude-usage-overlay/config.json`
2. Change `"polling_interval": 60` (1 minute)
3. Restart app
4. Verify updates happen more frequently (watch panel label update)

**Test 6: Custom Thresholds**
1. Edit config to set `"session_thresholds": [30, 60, 80]`
2. Restart app
3. If current usage is above 30%, verify notification appears (or check notifier is using new thresholds via log)

**Expected Results:**
- All menu items functional
- Config file updates persist
- Autostart .desktop file creates/removes correctly
- Custom config values are respected after restart
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 test cases pass with user confirmation.
</verification>

<success_criteria>
- User confirms Settings menu is complete and functional
- User confirms pause notifications works
- User confirms autostart creates/removes .desktop file
- User confirms custom polling interval takes effect
- User confirms custom thresholds are respected
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-polish/04-03-SUMMARY.md`
</output>
